// local dependency -- use it only in the current project
// npm i <package name>
//
// global dependency -- use it in any project
// npm install -g <package name

// npm init -y --create a package json file

// npm i <package name> -D ---- INSTALL DEV DEPENDENCIES.
// Dev dependencies are packages that we use to develop the app but we dont need these packages to make the app work in production